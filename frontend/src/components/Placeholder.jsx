export default function Placeholder({ label }) {
  return (
    <div className="bg-white rounded-xl2 shadow-soft p-6">
      <h3 className="text-[15.5px] font-bold mb-2">{label}</h3>
      <p className="text-[13.5px] text-muted">This section isn't built out in the prototype yet.</p>
    </div>
  )
}
